worker_processes auto;
error_log /dev/stderr notice;


events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    client_max_body_size 50M;

    # Oculta a versão do Nginx nos headers de resposta e páginas de erro
    server_tokens off;

    # Non-root temporary paths
    client_body_temp_path /tmp/nginx_client_body;
    proxy_temp_path       /tmp/nginx_proxy;
    fastcgi_temp_path     /tmp/nginx_fastcgi;
    uwsgi_temp_path       /tmp/nginx_uwsgi;
    scgi_temp_path        /tmp/nginx_scgi;
    
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    
    access_log /dev/stdout main;
    error_log /dev/stderr notice;
    sendfile on;
    keepalive_timeout 65;
    
    server {
        listen 8080;
        server_name _;

        root /app/dist/deppi;
        index index.html;

        # Cabeçalhos de segurança (aplicados a todas as respostas deste server)
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        # O hash abaixo autoriza o script inline de src/index.html que aplica o
        # tema salvo (localStorage) em <html> antes do primeiro paint, evitando
        # o "flash" de tema claro seguido de escuro. Se esse script for editado,
        # o hash precisa ser recalculado (o próprio erro do CSP no console do
        # navegador informa o novo valor sha256-... a usar).
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'sha256-B2P1tmwMAiI/gL7Q19i7kjsx4JcFoIXWeGVckGj75fU=' https://www.googletagmanager.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com; frame-src 'self' https://www.youtube.com https://player.vimeo.com;" always;

        location / {
            try_files $uri $uri/ /index.html;
        }

        # Standard MIME type mapping from mime.types
        location ~* \.js$ {
            try_files $uri =404;
            access_log off;
        }

        location /health {
            proxy_pass http://127.0.0.1:3000/health;
        }

        location /api/ {
            proxy_pass http://127.0.0.1:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
        
        location /uploads/ {
            proxy_pass http://127.0.0.1:3000;
        }
    }
}
